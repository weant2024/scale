// Importa a biblioteca React
import React from 'react';

// Define o componente ConfigPersonalizada como uma função de componente
const ConfigPersonalizada = () => {
    return (
        <section id="config">
            <h2>Configuração Personalizada</h2>
            <p>Aqui vão informações sobre as configurações personalizadas.</p>
        </section>
    );
}

// Exporta o componente ConfigPersonalizada como o padrão do módulo
export default ConfigPersonalizada;
