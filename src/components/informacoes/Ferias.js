// Importa a biblioteca React
import React from 'react';

// Define o componente Ferias como uma função de componente
const Ferias = () => {
    return (
        <section id="ferias">
            <h2>Férias</h2>
            <p>Aqui vão detalhes sobre as férias do usuário.</p>
        </section>
    );
}

// Exporta o componente Ferias como o padrão do módulo
export default Ferias;
