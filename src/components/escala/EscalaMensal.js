// Importa a biblioteca React
import React from 'react';

// Define o componente EscalaMensal como uma função de componente
const EscalaMensal = () => {
    return (
        <section id="escala_mensal">
            <h2>Escala Mensal</h2>
            <p>Aqui vão informações sobre a escala mensal.</p>
        </section>
    );
}

// Exporta o componente EscalaMensal como o padrão do módulo
export default EscalaMensal;
