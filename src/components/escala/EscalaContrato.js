// Importa a biblioteca React
import React from 'react';

// Define o componente EscalaContrato como uma função de componente
const EscalaContrato = () => {
    return (
        <section id="escala_contrato">
            <h2>Escala Contrato</h2>
            <p>Aqui vão informações sobre a escala de contrato.</p>
        </section>
    );
}

// Exporta o componente EscalaContrato como o padrão do módulo
export default EscalaContrato;
